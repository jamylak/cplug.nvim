{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug fixture app",
      "type": "lldb",
      "request": "launch",
      "program": "${workspaceFolder}/build/cplug_fixture_existing_launch",
      "cwd": "${workspaceFolder}"
    }
  ]
}
